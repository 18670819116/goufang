<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle">
    <!-- 填充 -->
    <solid android:color="@color/main_color"/>
    <!-- 圆角 -->

    <corners
        android:radius="@dimen/y40"
        />
    <size
        android:width="@dimen/x320"
        android:height="@dimen/y80"/>
</shape>