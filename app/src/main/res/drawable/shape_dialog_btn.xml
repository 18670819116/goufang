<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle">
    <!-- 填充 -->
    <solid android:color="@color/main_color"/>
    <!-- 圆角 -->

    <corners
        android:radius="@dimen/y26"
        />
    <size
        android:width="@dimen/x232"
        android:height="@dimen/y52"/>
</shape>